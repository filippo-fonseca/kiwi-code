# Kiwi Code divergence from upstream

This file is the complete, enumerated record of every upstream (`pingdotgg/t3code`)
file that Kiwi Code modifies, and why. It is the map used during the weekly
`git merge upstream/main` to know where and why we differ.

**Every future Kiwi Code PR MUST keep this table current.** If a PR touches a
file that upstream owns, add or update its row here in the same PR. If a PR stops
diverging on a file (for example, upstream adopts our change), remove its row.
A PR that changes upstream files without updating this table is incomplete.

## Modified files

| File                                                                     | What changed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Why                                                                                                                                                                                                                |
| ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `KIWI.md`                                                                | New file (Kiwi-only).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Documents what Kiwi Code is and the fork-sync model.                                                                                                                                                               |
| `KIWI-DIVERGENCE.md`                                                     | New file (Kiwi-only).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | This divergence record itself.                                                                                                                                                                                     |
| `apps/web/src/branding.ts`                                               | Default `APP_BASE_NAME` changed from `"T3 Code"` to `"Kiwi Code"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Rebrand the web app product name. The value is still injectable from the desktop bridge, so only the fallback default moved.                                                                                       |
| `apps/desktop/src/app/DesktopEnvironment.ts`                             | Module-level `APP_BASE_NAME` constant changed from `"T3 Code"` to `"Kiwi Code"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Rebrand the desktop app display name (drives window title, About panel, dock name).                                                                                                                                |
| `apps/desktop/scripts/electron-launcher.mjs`                             | `APP_DISPLAY_NAME` changed from `"T3 Code (Dev)"` / `"T3 Code (Alpha)"` to `"Kiwi Code (Dev)"` / `"Kiwi Code (Alpha)"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Rebrand the dev/packaged launcher bundle name (macOS `.app` display name and helper bundle names).                                                                                                                 |
| `apps/desktop/package.json`                                              | `productName` changed from `"T3 Code (Alpha)"` to `"Kiwi Code (Alpha)"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Rebrand the electron-builder product name for packaged Alpha builds.                                                                                                                                               |
| `scripts/build-desktop-artifact.ts`                                      | `DESKTOP_APP_ID` changed from `"com.t3tools.t3code"` to `"com.filippofonseca.kiwicode"`; added `KIWI_DEFAULT_UPDATE_REPOSITORY = "filippo-fonseca/kiwi-code"`; `resolveDesktopProductName` nightly fallback changed to `"Kiwi Code (Nightly)"` and default fallback to `"Kiwi Code"`; `resolveGitHubPublishConfig` now falls back to the Kiwi repo when no env override is set; macOS build `protocols` display name changed from `"T3 Code"` to `"Kiwi Code"` (deep-link schemes `t3code`/`t3code-dev` left intact).                                                                                                                                                       | Give packaged Kiwi builds their own bundle identity and point the auto-update feed at the Kiwi repo so it can never resolve upstream (pingdotgg) releases.                                                         |
| `apps/web/src/branding.test.ts`                                          | Updated the derived-default `APP_DISPLAY_NAME` assertion from `"T3 Code (Nightly)"` to `"Kiwi Code (Nightly)"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Keep the branding test in sync with the rebranded default constant.                                                                                                                                                |
| `apps/desktop/src/app/DesktopAppIdentity.test.ts`                        | Updated `setName` / About-panel `applicationName` assertions from `"T3 Code (Alpha)"` to `"Kiwi Code (Alpha)"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Keep the identity test in sync with the rebranded default display name.                                                                                                                                            |
| `scripts/build-desktop-artifact.test.ts`                                 | Updated `appId`, passkey entitlements, and mac protocol-name assertions to the Kiwi identity; added a test that the publish config defaults to `filippo-fonseca/kiwi-code` with no env.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Keep the artifact-builder tests in sync with the rebranded fork identity and update feed.                                                                                                                          |
| product-name prose across `apps/web/src` + `apps/desktop/src` (26 files) | Renamed the in-prose product-name literal `"T3 Code"` to `"Kiwi Code"` in user-facing copy and aria-labels: settings help text, theme/keybinding hints, update prompts, version-skew hints, provider/preview messages, WSL/SSH error messages, application-menu copy, relay/link environment messages, and mobile-client labels. Updated the tests that assert those strings (`versionSkew.test.ts`, `desktopUpdate.logic.test.ts`, `MobileClientsUserProfilePage.logic.test.ts`, `DesktopSshEnvironment.test.ts`). Left `T3 Connect` (a distinct service name), URLs, `T3CODE_*` env prefixes, `t3code://` schemes, bundle/`.app` names, and userData dir names untouched. | Finish the Phase 0 rebrand copy pass so the visible product name matches the authoritative `APP_BASE_NAME`, without disturbing the distinct `T3 Connect` service, functional identifiers, or state-migration keys. |

### Phase 1 (bgsd observability) touchpoints

New Kiwi-only code (no upstream conflict surface): `packages/contracts/src/bgsd.ts`, `apps/server/src/bgsd/` (discovery, bridgeClient, fsWatcher, projection, service + tests), `apps/web/src/kiwi/` (feed, realFeed, bgsdStore, kiwiTheme, dashboard components, status bar, sidebar section + tests).

| File                                         | What changed                                                                                                                            | Why                                                                                                                                   |
| -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/contracts/src/index.ts`            | Added `export * from "./bgsd.ts"`.                                                                                                      | Expose the bgsd contracts alongside upstream contracts.                                                                               |
| `packages/contracts/src/rpc.ts`              | Import from `./bgsd.ts`; `WS_METHODS.subscribeBgsdWorkspace`; `WsSubscribeBgsdWorkspaceRpc` declaration; registration in `WsRpcGroup`.  | Declare the streaming RPC channel the Kiwi UI subscribes to.                                                                          |
| `apps/server/src/server.ts`                  | Import + `Layer.provideMerge(BgsdWorkspaceService.layer)` in `RuntimeDependenciesLive`.                                                 | Provide the bgsd workspace service to the server runtime.                                                                             |
| `apps/server/src/ws.ts`                      | Import, auth scope requirement, service acquisition in `makeWsRpcLayer`, and the `subscribeBgsdWorkspace` stream handler.               | Serve the bgsd push stream over the existing `/ws` Effect RPC socket.                                                                 |
| `apps/server/src/server.test.ts`             | `BgsdWorkspaceService.layer` added to the test runtime stack.                                                                           | The ws RPC layer now requires the service; tests need it provided.                                                                    |
| `apps/web/src/rightPanelStore.ts`            | Registered singleton `kiwi` surface kind (kinds array, union member, singleton case).                                                   | Plug the Kiwi Dashboard into the existing right-panel surface system.                                                                 |
| `apps/web/src/components/RightPanelTabs.tsx` | `Bot` icon import, title/icon cases for `kiwi`, `onAddKiwi`/`kiwiAvailable` props, empty-state and `+`-menu entries.                    | Render and open the Kiwi surface like any other panel.                                                                                |
| `apps/web/src/components/ChatView.tsx`       | `addKiwiSurface` + `kiwiAvailable`, `KiwiDashboard` render case, `KiwiStatusBar` mount above the composer, open-surface request effect. | Wire the Kiwi surface and status bar into the main view.                                                                              |
| `apps/web/src/components/Sidebar.tsx`        | Import + `<KiwiSidebarSection />` mount between Search and projects.                                                                    | Show bgsd sessions in the sidebar.                                                                                                    |
| `packages/client-runtime/src/rpc/client.ts`  | Added `subscribeBgsdWorkspace` to the `EnvironmentSubscriptionRpcTag` union.                                                            | Admit the bgsd push channel as a durable per-environment subscription so the client `subscribe()` helper and atom families accept it. |
| `apps/web/src/routes/__root.tsx`             | One bootstrap `useEffect` in `EventRouter`: `connectBgsdFeed(bgsdRealFeed)` at connection scope, skipped when `isFixtureFeedEnabled()`. | The single touchpoint that wires the Kiwi UI to the real server push channel (fixture wins when its dev flag is set).                 |

## Deliberately NOT changed (documented decisions)

- **App id / bundle id at runtime (`com.t3tools.t3code`).** The macOS/Windows/Linux
  runtime bundle identifiers in `apps/desktop/src/app/DesktopEnvironment.ts`
  (`appUserModelId`, `linuxDesktopEntryName`, `linuxWmClass`) and in
  `apps/desktop/scripts/electron-launcher.mjs` (`APP_BUNDLE_ID`,
  `APP_PROTOCOL_SCHEMES` such as `t3code`) were left as-is. These are protocol
  handlers, OS registration identifiers, and deep-link schemes. Changing them
  risks breaking dev-mode launch, deep links, and single-instance behavior, and
  they are internal identifiers rather than user-visible product name. Only the
  packaged build `appId` (in `build-desktop-artifact.ts`, which is what ships to
  end users) was rebranded to `com.filippofonseca.kiwicode`. Aligning the runtime
  identifiers and protocol schemes is deferred to a later phase.
- **userData directory names (`t3code`, `t3code-dev`, and legacy `T3 Code (Alpha)`).**
  `userDataDirName` / `legacyUserDataDirName` in `DesktopEnvironment.ts` were left
  unchanged. Renaming the userData directory would orphan any existing user
  settings/state and there is legacy-path migration logic keyed on these names.
  This is a state-migration concern, not a simple config change, so it is deferred.
- **Environment variable prefixes (`T3CODE_*`) and telemetry/span keys**
  (for example `desktop.appIdentity.configure`, `t3codeCommitHash`,
  `@t3tools/desktop/...` service tags). Left unchanged: these are internal
  contract keys, config env names, and observability identifiers. Renaming them
  would break configuration and telemetry continuity with no user-facing benefit.
- **`T3 Connect` (the distinct service/feature name).** All `"T3 Connect"` copy in
  `ConnectionsSettings.tsx`, `RelayClientInstallDialog.tsx`, and elsewhere is left
  as-is. It names a separate service (relay/tunnel networking), not the app, so it
  is intentionally out of scope for the product-name rebrand.

## Pending

- **Icons.** Kiwi Code still ships the upstream t3code icons
  (`apps/desktop/resources/`, `assets/`, `favicon.svg`, brand asset paths in
  `build-desktop-artifact.ts`). Kiwi-branded icons are pending and were
  intentionally not designed in this phase.
